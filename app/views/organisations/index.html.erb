<p id="notice"><%= notice %></p>
<% if user_signed_in? && current_user.is_volonteer? %>
  <div class="page-header-my">
    <h1 class="text-center title">STREFA WOLONTARIUSZA</h1>
  </div>
  <ol class="breadcrumb">
    <li><%= link_to 'Strona główna', home_index_path %></li>
     <li><%= link_to 'Strefa wolontariusza', volonteer_home_index_path %></li>
    <li class="active">Wyszukaj organizację</li>
  </ol>
  <nav>
      <div class="container-fluid">
          <div class"navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
                <span class="sr-only"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
           </div>
      </div>
      <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
          <ul class="nav nav-justified nav-vol nav-all menu-text">
            <li class="active">
                <%= link_to 'Strona główna', home_index_path %>
            </li>
            <li class="active">
              <%= link_to 'Zarejestruj się', new_user_registration_path %>
            </li>
            <li class="active">
              <%= link_to 'Wyszukaj organizację', new_search_organisation_path %>
            </li>
            <li class="active">
              <%= link_to 'Wyszukaj osobę prywatną', search_individuals_path %>
            </li>
          </ul>
      </div>
   </nav>
<% end %>
<div class= "tab">
<table class="table table-bordered table-hover table-condensed table-responsive">
  <thead>
    <tr class="th">
      <th>Nazwa</th>
      <th>Miasto</th>
      <th>Rodzaj wolontariatu</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody class="form">
    <% @organisations.each do |organisation| %>
      <tr>
        <td><%= organisation.name %></td>
        <td><%= organisation.city %></td>
        <td><%= organisation.volontary_type.name %></td>
        <td><%= link_to 'Zobacz', organisation %></td>
      </tr>
    <% end %>
  </tbody>
</table>
</div>
</div>
<br>

