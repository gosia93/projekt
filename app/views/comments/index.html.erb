<%= render 'menu/menu_admin' %>
   <div class="tab">
<table class="table table-bordered table-hover table-condensed table-responsive">
  <thead>
    <tr class="th">
      <th>Content</th>
      <th>Status</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody class="form">
    <% @comments.each do |comment| %>
      <tr>
        <td><%= comment.content %></td>
        <td><%= comment.status %></td>
        <td><%= link_to 'Edytuj', edit_comment_path(comment) %></td>
        <td><%= link_to 'Usuń', comment, method: :delete, data: { confirm: 'Jesteś pewien?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
</div>
